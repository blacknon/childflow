backend = "rootless-internal"
default_policy = "allow"
flow_log = "./logs/domain-deny-origin-exact.jsonl"
deny_domains_exact = ["origin-http.exact.invalid"]
command = [
  "curl",
  "--connect-timeout",
  "5",
  "--max-time",
  "15",
  "-fsS",
  "http://origin-http.exact.invalid:8080/",
]
