backend = "rootless-internal"
default_policy = "allow"
flow_log = "./logs/domain-deny-origin.jsonl"
deny_domains = ["origin-http.demo"]
command = [
  "curl",
  "--connect-timeout",
  "5",
  "--max-time",
  "15",
  "-fsS",
  "http://origin-http.demo:8080/",
]
